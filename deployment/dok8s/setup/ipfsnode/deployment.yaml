apiVersion: apps/v1
kind: Deployment
metadata:
  name: ipfsnode
  labels:
    app: ipfsnode
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ipfsnode
  template:
    metadata:
      labels:
        app: ipfsnode
    spec:
      containers:
      - name: ipfsnode
        image: ipfs/go-ipfs:v0.4.23
        env:
        - name: IPFS_PROFILE
          value: server
        - name: IPFS_PATH
          value: /data 
        ports:
        - containerPort: 4001
